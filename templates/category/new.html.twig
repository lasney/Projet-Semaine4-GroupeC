{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
<div class="container">
    <div class="row"><h1>Create new Category</h1></div>
    <div class="row">
        <div class="col-sm-1"></div>
        <div class="col-sm-1"></div>
        <div class="col-sm-8">
		
		{{ include('category/_form.html.twig') }}

		<a class="btn btn-success" href="{{ path('category_index') }}">back to list</a>
		</div>
        <div class="col-sm-1"></div>
        <div class="col-sm-1"></div>
    </div>
</div>
    

    
{% endblock %}
